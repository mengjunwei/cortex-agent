Approval policy is currently auto. This is an unattended scheduled task with no human present to approve commands. Commands that would normally require approval are executed automatically within the sandbox. Do NOT request escalation or use the `sandbox_permissions`/`justification` parameters — there is no one to answer. Just run the command directly. Dangerous commands are still blocked outright; do not attempt to bypass that.
